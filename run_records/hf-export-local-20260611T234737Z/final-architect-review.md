ARCHITECTURE REVIEW REPORT

Architectural Status: CLEAR

Concerns with file:line evidence and concrete recommendations.
- The missing-ID fallback/order-sensitivity WATCH item is resolved. `source_id_for()` now falls back to `row-{stable_json_hash(source_record or row)}` when no explicit ID exists (scripts/kmmad_hf_export.py:226-235), and export fails closed on missing or duplicate normalized `record_id`s before any write (scripts/kmmad_hf_export.py:644-656). The regression covers forward/reverse order stability and content-addressed IDs (tests/test_hf_export.py:306-353). Verification shows the suite, compileall, ruff, pyright, and HF export smokes all passed (run_records/hf-export-local-20260611T234737Z/final-fix3-verification.log:172-218). Recommendation: keep the regression as a permanent guardrail.
- The same-named root ambiguity WATCH item is resolved. `build_input_root_labels()` assigns invocation-local portable labels like `input-00-*` and `input-01-*` (scripts/kmmad_hf_export.py:340-357), and those labels are what the manifest records and what `translation_artifact_path` uses (scripts/kmmad_hf_export.py:620-683). The regression verifies both the manifest labels and the row-level paths for duplicate-named roots (tests/test_hf_export.py:355-402). Recommendation: no blocker; retain the portable-label contract.
- Schema/provenance/secret safety is acceptable for this local HF export pipeline. All export records pass through sanitization before persistence (scripts/kmmad_hf_export.py:99-103, 272-305), and validation scans loaded dataset rows, JSONL shards, the manifest, the dataset card, and any prior validation artifact for secret-like values and duplicate IDs (scripts/kmmad_hf_export.py:491-585). The shared helpers explicitly redact secret-looking strings and secret field names (scripts/kmmad_common.py:24-46, 81-138). The regression proves redaction plus tamper detection in both data shards and loaded datasets (tests/test_hf_export.py:142-219). Recommendation: no blocker for the local packaging gate.
- QC/release semantics are intentionally declarative, not upload-enforcing. The manifest records `qc_attestation_present` and `release_ready` from the declared QC report/status (scripts/kmmad_hf_export.py:687-689), while the runbook says production release requires a machine-readable QC report path and otherwise the export remains non-release packaging (docs/kmmad_translation_smoke_runbook.md:240-242, 272-276). Recommendation: treat `release_ready` as advisory metadata; if you ever want stricter semantics, validate the report path separately. This is not a current blocker.
- Dependency/provenance surface stays narrow. `pyproject.toml` only adds `datasets` (pyproject.toml:7-9), and `uv.lock` pins that package plus transitive dependencies without introducing any secret-bearing configuration in the reviewed stanza (uv.lock:564-573). Recommendation: keep the lockfile as-is; no provenance risk surfaced here.
- Run-record provenance looks acceptable on spot check. The final verification log is a test/tooling provenance artifact with passed checks and manifest excerpts, not raw payload dumps (run_records/hf-export-local-20260611T234737Z/final-fix3-verification.log:172-218), and the cleanup note records secret-scan regressions plus the bounded remaining risk of not running full-scale local exports (run_records/hf-export-local-20260611T234737Z/quality-ai-slop-cleaner.md:17-35). Recommendation: keep these artifacts as provenance evidence, but continue avoiding raw env dumps or data payload blobs in run records.

SYNTHESIS: architect status: CLEAR
